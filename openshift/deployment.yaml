apiVersion: apps/v1
kind: Deployment
metadata:
  name: jackie-cronjob
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jackie-cronjob
  template:
    metadata:
      labels:
        app: jackie-cronjob
    spec:
      imagePullSecrets:
        - name: dockerhub-pull
      containers:
      - name: jackie-cronjob
        image: DOCKER_IMAGE_REPLACE_ME
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 5
